*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Twilio credentials
.env.twilio
*-credentials.php
*-config-local.php
.last_deploy_sha

# Despejos da base de dados. Nunca entram no repositório: o farmasequel.sql
# que estava na raiz tinha 653 MB de dados reais de clientes.
*.sql
*.sql.gz
/storage/app/db-dump

# Trabalho de diagnóstico e ficheiros de rascunho
/diag*.txt
/diag*.log
/d.log
/logs.txt
/tmp
/scratchpad*.php
/sma_products_schema.txt
*.tmp.php

# Renderizações de marketing (anúncios, capturas de ecrã para redes sociais).
# Não é código: sai de ferramentas de imagem e volta a sair quando for preciso.
/output

# Sessões e worktrees do Claude Code
/.claude

# Build do instalador offline (payload/output — nunca versionar)
/installer/payload/
/installer/dist/
/installer/soserp-tray.exe
/installer/soserp.ico

# Bancada do PWA (Playwright)
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# Contagem de versão do PWA (por instalação, não versionada)
/storage/app/pwa-versao.json

# Construção dos ecrãs em React (public/js/react/app.js e os pedaços).
# Sai do `npm run react:build` e vai para produção por FTP, como os outros
# ficheiros de public/js — não entra no repositório.
/public/js/react
/public/react
/public/pwa-app

# Pacotes de recuperacao (git bundle). Ficam no disco, nunca no repositorio:
# contem a historia inteira, credenciais incluidas.
/recovery/
